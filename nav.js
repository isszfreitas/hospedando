window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#header')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})

window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#nav')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})

window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#nav-logo')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})

window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#nav-logo-1')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})

window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#navbar')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})

window.addEventListener("scroll", function(){
    let nav = this.document.querySelector ('#nav-btn')
    nav.classList.toggle('rolagem',window.scrollY > 0)
})
